{
  "rules": {
    ".read": false,
    ".write": false,

    "cfg": {
      ".read": "auth != null",
      ".write": "auth != null && auth.token.email === 'alissonh26@gmail.com'"
    },

    "meta": {
      ".read": "auth != null",
      ".write": "auth != null && auth.token.email === 'alissonh26@gmail.com'"
    },

    "appts": {
      ".read": "auth != null",
      "$apptId": {
        ".write": "auth != null"
      }
    },

    "publicProfiles": {
      "$slug": {
        ".read": true,
        ".write": "auth != null && auth.token.email === 'alissonh26@gmail.com'"
      }
    }
  }
}
